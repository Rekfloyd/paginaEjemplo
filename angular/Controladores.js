angular.module("Pagina")/* en este caso no estamos redefiniendo un modulo solo lo invocamos y agregamos un  controlador*/
		.controller("Objetos", ["$scope","$routeParams","$resource",function (a,r,p){ /*A R P SON nomenicos para scope route etc...*/
			a.comentario = ""; /*declaramos cadena vacia para nombre*/
}]);